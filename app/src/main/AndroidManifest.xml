<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jagger.alertobarangay">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlertoBarangay"
        tools:ignore="AllowBackup">
        <activity android:name=".PhoneNumberWebAppConnection"></activity>
        <activity android:name=".Register" />
        <activity android:name=".Profile" />
        <activity android:name=".History" />
        <activity android:name=".Dashboard" />
        <activity android:name=".Login" />
        <activity android:name=".EmergencyReport" />
        <activity android:name=".OtpVerification" />
        <activity android:name=".SignUp" />
        <activity android:name=".LandingPage" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AlertoBarangay.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".Receive_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>